# NAS

The NAS currently runs as a Debian LXC with Samba.

The present design is deliberately simple and migration-friendly. Configuration examples may be added here once sanitized, but user data, credentials and storage snapshots must never be committed.
