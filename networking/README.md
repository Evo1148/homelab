# Networking

This directory will contain sanitized network documentation and reusable configuration examples.

Production IPs, VPN credentials, controller backups and private keys are intentionally excluded.
