# The live Beszel agent environment belongs in agent.env and must not be committed.
#
# Copy this file to agent.env, then add only the variables required by your
# Beszel agent pairing/configuration.
#
# Do not place real keys, tokens or secrets in this example file.
