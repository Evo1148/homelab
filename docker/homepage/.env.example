# File ownership inside the container
PUID=1000
PGID=1000

# Published host port
HOMEPAGE_PORT=3000

# Use the internal hostname or host:port that should be accepted by Homepage.
# Example only — replace locally with the real HomeLab value.
HOMEPAGE_ALLOWED_HOSTS=homepage.home.arpa:3000
