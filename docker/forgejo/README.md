# Forgejo

Forgejo provides self-hosted Git inside the HomeLab.

The current deployment has been validated end-to-end with SSH authentication and a real clone → commit → push workflow.

Do not commit SSH private keys, user tokens, databases or repository data to this public repository.
